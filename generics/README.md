# Generics in Dart

**Generics** allow you to write reusable code that works with different data types while keeping **type safety**.

Instead of creating separate classes or methods for each type (`int`, `String`, etc.), you can define a **generic type** `<T>` and specify the type when using it.

📌 **In short:**
- Write code once, use it with any type.
- Let the compiler catch type errors early.
- Reduce runtime bugs.

Check `example.dart` for simple examples using generics.
